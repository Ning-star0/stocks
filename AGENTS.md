# AI Repository Instructions

本仓库是一个以证据为基础的 AI 股票研究与条件化交易决策系统。

## 强制阅读

在分析、规划或修改任何与产品、数据、AI、股票分析、新闻、K 线、基本面、公告、交易、风险、回测、页面状态或任务调度有关的内容前，必须完整阅读：

- `docs/AI_INVESTMENT_SYSTEM_NORTH_STAR.md`

该文件是本项目的强制性产品与工程规范。除用户在当前任务中明确提出的新要求外，不得违反、弱化或绕过它。

## 每次任务的必做事项

1. 开始时说明本次工作如何服务“基于完整可信证据生成条件化、可审计、扣费后正期望值计划”的用户初衷。
2. 将工作归类为：数据完整度、决策质量、风险控制、验证可信度或必要基础设施。
3. 优先处理北极星文档 P0；P0 未完成前，不主动扩张无关模块。
4. AI 只负责阅读、归纳、比较和解释；数值、时效、费用、仓位、风险、复权、门控和状态必须由确定性代码计算。
5. 数据缺失、过期、冲突或 fallback 必须对用户可见，并能阻止不合格的新增买入。
6. 禁止依靠中文文本正则作为最终买卖状态来源；使用结构化枚举和服务端校验。
7. 禁止为了增加买入信号、提高回测收益或让页面更“积极”而降低硬风控。
8. 禁止引入未来数据、训练/测试污染和未计费用的绩效结论。
9. 修改前检查并保护用户未提交改动；不覆盖无关工作。
10. 完成后执行北极星文档第 11、12、14 节的检查与汇报。

## 停止条件

出现以下情况时，不得自行假设并继续：

- 用户要求与北极星核心方向冲突；
- 新数据源的合法性、口径或时效无法确认；
- 需要弱化硬否决才能实现目标；
- 无法排除未来函数、缓存污染或结论使用错误数据；
- 需要自动下单、承诺收益或扩大真实资金权限。

此时应说明冲突、风险和可选方案，请求用户决定。

